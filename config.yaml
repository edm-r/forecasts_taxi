paths:
  raw_data: data/raw
  processed_data: data/processed
  features_data: data/features

model:
  learning_rate: 0.05
  n_estimators: 500
  max_depth: 6
  objective: regression
  seed: 42

pipeline:
  n_jobs: -1
  chunk_size: 100000
